# zhurl Agent Instructions

## 经验总结

### 文档语言必须先对齐项目和用户语境

写项目文档前，必须先判断当前仓库和用户要求的主要语言。即使技术名词、命令和字段名保持英文，说明文字、标题、目录和注释也要跟随项目上下文与用户表达；不能因为 API 文档天然接近英文生态，就默认整篇使用英文。

例子：用户要求整理某个中文产品的常用接口文档时，正确做法是用中文写标题、描述、参数解释、注意事项和返回说明，只在 URL、JSON 字段、命令选项中保留英文原文；如果直接写成英文文档，会让文档的主要读者和使用场景错位。
